#!/bin/bash

# Setups up the Spelling App.
# You only need to run this once, then you can use py app.py to start.
# Must have Python 3.8 or later installed.

pip install -r requirements.txt
python setup_db.py